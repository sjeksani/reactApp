import React from 'react';

var Heading = React.createClass({
            render: function() {
                return ( < h3 > Fill the form now < /h3>);
                }
            });

export default Heading;

